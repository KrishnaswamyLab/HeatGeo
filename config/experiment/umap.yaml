# @package _global_

#defaults: 
#  - all_data

hydra:
  sweeper:
    params:
      name : umap
      model : umap
      model.n_neighbors : 10,15
      model.n_components : 2
      model.min_dist : 0.1,0.5,0.99